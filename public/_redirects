# Photo URL routing (/album/path/i-imageId) is handled by the Cloudflare Pages
# Function at functions/[[path]].js, which only runs for URLs with no matching
# static file. No _redirects rules are needed for photo URLs.
